// We make a constructor which defines what the product is supposed to contain

class product {
    constructor(title, price, brand, thumbnail,) {
      this.title = title;
      this.price = price;
      this.brand = brand;
      this.thumbnail = thumbnail;
    }
  }
  
  
  // Export the product
module.exports = product;
  